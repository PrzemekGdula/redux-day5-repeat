import React from 'react'

import { connect } from 'react-redux'

const Chat = (props) => (
    <div>
        <input
            value {}
            onChange={}
        />
    </div>
)

export default connect(

)(Chat)